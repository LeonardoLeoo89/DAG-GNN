[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dag_gnn"
version = "0.1.0"
dependencies = []

[tool.setuptools.package-dir]
"dag_gnn" = "src"
